name: renovate
on:
  schedule:
  - cron: '0/30 * * * *'
jobs:
  renovate:
    uses: eidp/actions-common/.github/workflows/renovate.yml@1d37ef097c48440e0d87e70b0849bfa1b09cc961 # v0
    with:
      runs-on: dind-runner
      renovate-config-repo: renovate-config
    secrets:
      GITHUB_APP_ID: ${{ secrets.DEP_MANAGER_GITHUB_APP_ID }}
      GITHUB_APP_PEM_FILE: ${{ secrets.DEP_MANAGER_GITHUB_APP_PEM_FILE }}
