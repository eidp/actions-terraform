name: renovate
on:
  schedule:
  - cron: '0/30 * * * *'
jobs:
  renovate:
    uses: eidp/actions-common/.github/workflows/renovate.yml@5432677a2aac2a4e09787d68519acb6617f65197 # v0.6.1
    with:
      runs-on: dind-runner
      renovate-config-repo: renovate-config
    secrets:
      GITHUB_APP_ID: ${{ secrets.DEP_MANAGER_GITHUB_APP_ID }}
      GITHUB_APP_PEM_FILE: ${{ secrets.DEP_MANAGER_GITHUB_APP_PEM_FILE }}
